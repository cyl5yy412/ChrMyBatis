<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lnsoft.mapper.ItemMapper">
    <!-- 首先加载到内存 -->
    <select id="selectByPrimaryKey" resultType="com.lnsoft.dataobject.ItemInfo">
        select * FROM iteminfo WHERE id = ?
    </select>
    <select id="selectAll" resultType="com.lnsoft.dataobject.ItemInfo">
        SELECT * FROM iteminfo
    </select>
</mapper>